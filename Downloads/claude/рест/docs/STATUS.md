# STATUS — ИИ чат-бот Numero Uno Steakhouse

> **Для Claude и человека**: это первый файл в новой сессии. После прочтения
> **обязательно** сверить с git: `git tag -l "stage-*"`, `git log --oneline -20` —
> таблица ниже может устареть.

**Последнее обновление**: 2026-07-11 (бутстрап плана; идёт продажа, готовим демо)
**Текущий этап**: Этап 0 — Каркас демо (☐ не начат)
**Следующий шаг**: получить от пользователя ключ OpenRouter, поднять проект на Vercel
(страница + `/api/chat`), см. acceptance этапа 0 в [`07_ROADMAP.md`](07_ROADMAP.md).

---

## Сводка этапов

Фокус — **только демо** (концентрат для закрытия сделки). Полный проект (пакеты КП) —
в бэклоге ROADMAP, разворачиваем после согласия клиента.

Легенда: ☐ не начат · 🚧 в работе · ✅ закрыт · ⏸ заблокирован

| # | Этап | Статус | Закрывающий тег | Последний коммит | Дата закрытия |
|---|---|---|---|---|---|
| 0 | Каркас демо | ☐ | `stage-0-done` | — | — |
| 1 | Движок диалога брони | ☐ | `stage-1-done` | — | — |
| 2 | Дизайн под их сайт + финал Ágora | ☐ | `stage-2-done` | — | — |
| 3 | Деплой и показ клиенту | ☐ | `stage-3-done` | — | — |

Детальные критерии приёмки — в [`07_ROADMAP.md`](07_ROADMAP.md).

## Активная работа

**Этап**: 0 — Каркас демо. Ещё не начат.
**Ветка**: `dev`
**Сводка задач**: поднять Vercel-проект (страница чата + serverless `/api/chat`),
подключить OpenRouter по ключу, первый деплой.
**Как верифицируем**: открыть ссылку Vercel с телефона, написать боту, получить ответ.

## Известные блокеры

Ждём от пользователя ключ OpenRouter для старта этапа 0.

## Контекст сделки (для будущих сессий)

- Клиент: OlegValencia (Kwork), 3 стейк-хауса в Валенсии. Сайт на Тильде (ES/EN),
  бронирование Ágora Bookings, WhatsApp один на 3 ресторана.
- Согласовано КП из 3 пакетов: Старт 25к / Оптимальный 70к / Под ключ 120к ₽.
- Клиент попросил демо, согласился его ждать. Возврат с решением — на следующей неделе.
- Демо (этапы 0–1) — бесплатный инструмент убеждения. Дизайн под их сайт.
- Green API — сторонний платный канал WhatsApp (~1.5–2к ₽/мес на клиенте).
- Ágora: публичные виджеты брони widget/1684 и widget/1117 с токенами; предзаполнение
  даты через URL `?date=YYYY-MM-DD`. Публичного API создания брони наружу нет —
  безкликовая бронь только реверсом (этап 6, хрупко).

## История закрытий

Пока пусто. Формат:
`YYYY-MM-DD — Этап N: <название> — tag stage-N-done — заметка с конкретикой верификации`.

## Протокол обновления

**Старт сессии**: читать этот файл → `git tag -l "stage-*"` (сверить с таблицей) →
`git log --oneline -10` → открыть ROADMAP на текущем этапе → работать.

**Взятие этапа**: обновить «Текущий этап» и «Активную работу» → 🚧 в таблице →
коммит `chore(status): start stage N`.

**Закрытие этапа**: все чекбоксы `[x]` и проверены → `git tag stage-N-done -m "..."`
→ ✅ + тег + коммит + дата в таблице → запись в «Историю закрытий» → новый текущий
этап → коммит `chore(status): close stage N`.

## Что НЕ хранится здесь

Длинные планы — в `07_ROADMAP.md`. TODO внутри этапа — таск-лист сессии. История
кода — `git log`. Этот файл — снимок состояния по этапам на один экран.
